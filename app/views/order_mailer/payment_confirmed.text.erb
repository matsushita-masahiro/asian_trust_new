【Asia Business Trust】入金確認のお知らせ

<%= @user.name.present? ? @user.name : @user.email %> 様

いつもお世話になっております。
この度は、ご注文いただきありがとうございます。

ご入金を確認いたしました。

■ ご注文詳細
注文番号: #<%= @purchase.id %>
注文日時: <%= @purchase.purchased_at.strftime('%Y年%m月%d日 %H:%M') %>

<% @purchase_items.each do |item| %>
・<%= item.product.name.length > 30 ? "#{item.product.name[0..29]}..." : item.product.name %> × <%= item.quantity %> : ¥<%= number_with_delimiter(item.unit_price * item.quantity) %>
<% end %>

商品代金計: ¥<%= number_with_delimiter(@subtotal) %>
<% if @shipping_fee && @shipping_fee > 0 %>
送料: ¥<%= number_with_delimiter(@shipping_fee) %>
<% end %>
<% if @admin_fee && @admin_fee > 0 %>
事務手数料: ¥<%= number_with_delimiter(@admin_fee) %>
<% end %>
<% if @tax_amount && @tax_amount > 0 %>
消費税: ¥<%= number_with_delimiter(@tax_amount) %>
<% end %>

合計金額: ¥<%= number_with_delimiter(@total_amount) %>

<% if @purchase.purchase_invoice&.clinic_reservation_status == 'not_reserved' %>
■ 次のステップ
入金確認が完了いたしました。クリニック予約をしてください。
<% end %>

ご不明な点がございましたら、お気軽にお問い合わせください。

---
Asia Business Trust
Email: <%= ENV['ADMIN_EMAIL'] || 'support@example.com' %>