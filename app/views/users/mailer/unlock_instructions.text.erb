アカウントロック解除のご案内

<%= @resource.email %> 様

【重要】アカウントがロックされました

セキュリティ保護のため、複数回のログイン失敗によりアカウントがロックされました。

アカウントのロックを解除するには、下記のURLにアクセスしてください。

ロック解除URL:
<%= unlock_url(@resource, unlock_token: @token) %>

【セキュリティのヒント】
今後のアカウントロックを防ぐために：
・正しいメールアドレスとパスワードを確認してください
・パスワードを忘れた場合は「パスワードを忘れた方」をご利用ください
・定期的にパスワードを変更することをお勧めします
・他人とパスワードを共有しないでください

【ご注意】
このメールにお心当たりがない場合は、このメールを無視してください。
第三者による不正アクセスの可能性がある場合は、すぐにサポートまでご連絡ください。

<% if defined?(InvoiceRecipient) && InvoiceRecipient.first %>
<% company = InvoiceRecipient.first %>
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
<%= company.name %>
<%= company.address %>
<% if company.tel.present? %>TEL: <%= company.tel %><% end %>
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
<% else %>
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
アジアビジネストラスト株式会社
東京都中央区銀座1-1-1
TEL: 03-1234-5678
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
<% end %>