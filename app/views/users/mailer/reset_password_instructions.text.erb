パスワード再設定のご案内

<%= @resource.email %> 様

パスワードの再設定をご希望いただき、ありがとうございます。
下記のURLにアクセスして、新しいパスワードを設定してください。

パスワード再設定URL:
<%= edit_password_url(@resource, reset_password_token: @token) %>

【ご注意】
・このメールにお心当たりがない場合は、このメールを無視してください
・上記のURLにアクセスして新しいパスワードを作成するまで、現在のパスワードは変更されません
・セキュリティのため、このリンクの有効期限は24時間です

<% if defined?(InvoiceRecipient) && InvoiceRecipient.first %>
<% company = InvoiceRecipient.first %>
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
<%= company.name %>
<%= company.address %>
<% if company.tel.present? %>TEL: <%= company.tel %><% end %>
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
<% else %>
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
アジアビジネストラスト株式会社
東京都中央区銀座1-1-1
TEL: 03-1234-5678
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
<% end %>